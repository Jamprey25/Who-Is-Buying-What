node_modules/
.DS_Store
*.log
.auto-commit.log

# Environment variables — never commit real keys
.env
.env.local
.env.*.local

# Next.js build output
.next/

# Polling state (runtime data)
data/

# LLM response disk cache (dev-only, opt-in via LLM_CACHE=1)
.cache/

# TypeScript incremental build cache
tsconfig.tsbuildinfo
